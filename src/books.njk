---
layout: base.njk
title: Книги
---

<div style="max-width: 800px; margin: 40px auto; padding: 0 20px;">
    <h1 style="text-align: center; margin-bottom: 40px;">Мои Книги</h1>

    <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 30px;">
        
        {# Цикл: Eleventy берет все файлы из папки books #}
        {% for book in collections.books %}
        <div style="border: 1px solid var(--border-color); padding: 20px; border-radius: 4px; background: var(--card-bg);">
            {% if book.data.cover %}
                <img src="{{ book.data.cover }}" alt="{{ book.data.title }}" style="width: 100%; height: auto; border-radius: 2px; margin-bottom: 15px;">
            {% endif %}
            
            <h3 style="margin: 0 0 10px 0;">{{ book.data.title }}</h3>
            <div style="font-size: 0.9rem; color: var(--secondary-text); margin-bottom: 15px;">
                {{ book.content | safe }}
            </div>

            {% if book.data.link %}
                <a href="{{ book.data.link }}" target="_blank" class="btn-subscribe" style="display: inline-block; text-align: center; font-size: 0.9rem;">Читать</a>
            {% endif %}
        </div>
        {% else %}
            <p style="text-align: center; grid-column: 1/-1;">Книги пока не добавлены.</p>
        {% endfor %}

    </div>
</div>