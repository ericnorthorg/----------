---
layout: base.njk
title: Блог
---

<div style="max-width: 800px; margin: 40px auto; padding: 0 20px;">
    <h1 style="text-align: center; margin-bottom: 40px;">Блог</h1>

    <div style="display: flex; flex-direction: column; gap: 30px;">
        {% for post in collections.blog | reverse %}
        <article style="border-bottom: 1px solid var(--border-color); padding-bottom: 30px;">
            <div style="font-size: 0.85rem; color: var(--accent-color); font-weight: 700;">
                {{ post.date | date("dd.MM.yyyy") }}
            </div>
            <h2 style="margin: 5px 0 15px 0;">
                <a href="{{ post.url }}" style="text-decoration: none; color: var(--text-color);">{{ post.data.title }}</a>
            </h2>
            {% if post.data.image %}
                <img src="{{ post.data.image }}" style="width: 100%; max-height: 300px; object-fit: cover; margin-bottom: 15px; border-radius: 4px;">
            {% endif %}
            <p>{{ post.data.description }}</p>
            <a href="{{ post.url }}" style="font-weight: 600; color: var(--accent-color);">Читать далее →</a>
        </article>
        {% else %}
            <p style="text-align: center;">Постов пока нет. Зайди в админку и создай первый!</p>
        {% endfor %}
    </div>
</div>